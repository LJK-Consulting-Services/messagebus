# Test-only dependencies. Install with: pip install -r requirements-dev.txt
-r requirements.txt

# In-memory Redis for the huddle done-gate tests (tests/test_huddle_gate.py).
# The suite skips itself cleanly if this is absent, so it's optional for the
# pure-function suites but required to exercise the Redis-backed gate logic.
fakeredis>=2.0
